# API server listen port (must match frontend when using a custom port)
PORT=8777

# Optional: full API origin for packaged desktop / non-default host.
# If unset, Tauri uses http://localhost:${PORT} from the build environment.
# VITE_API_BASE=http://127.0.0.1:8777
